import re

# A simple extension to spacy's sentence parser
# It does nothing currently
def sentence_parser(doc):
    #prev = doc[0].text
    #length = len(doc)
    #for index, token in enumerate(doc):
    #    if (token.text == ',' and index!=(length - 1)):
    #        doc[index+1].sent_start = True
    #    prev = token.text
    return doc

